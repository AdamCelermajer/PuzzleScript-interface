legendes:
{
  "0": "empty",
  "1": "wall",
  "2": "player",
  "3": "crate",
  "5": "target"
}

final_goal:
Push all crates onto the targets

"Movement":{
IF action is '<dir>' AND '0' is in <dir> of '2' THEN '2' moves <dir>,
IF action is 'W' AND '5' is above '2' THEN '2' moves W
}

"Pushing":{
IF action is 'S' AND '0' is below '3' THEN '2' and '3' both move S
}

